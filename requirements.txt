pydantic>=2.7
PyYAML>=6.0
numpy>=1.26
opencv-python-headless>=4.10
httpx>=0.27
fastapi>=0.115
uvicorn>=0.30
# uvicorn ships no WebSocket implementation of its own, and without one it
# answers every socket with 404 - which means every surface sits at
# "reconnecting" forever and no event ever arrives.
websockets>=12

# Optional: only for hardware.backend=esp32
# pyserial>=3.5

# Optional: only for display.face_screen.surface=pygame, the face the robot
# draws itself instead of opening a browser.
#   pip install pygame
# From pip, not apt: Raspberry Pi OS's python3-pygame came with an SDL that
# had no video backend at all, so every driver refused by name while X was
# answering other clients perfectly well. The wheel brings its own SDL.
# pygame>=2.5

# Optional: only for signs.hands.reader=mediapipe, the hand shapes a child
# can hold up. Cards need nothing - the marker dictionaries are built into
# opencv - and this is the expensive half, so it is off until a Pi has been
# measured with it (python tools/signs_check.py --hands).
# On a Raspberry Pi 4, the version matters: 1.x is compiled for a processor
# with AES instructions, which the BCM2711 has none of, and it does not
# raise - it aborts the process with an illegal instruction. 0.10.x was
# built before that.
#   pip install "mediapipe==0.10.18"
#   python tools/fetch_models.py --hands
#   python tools/doctor.py --mode pi      # says whether it runs on this one
# mediapipe>=0.10,<1
#
# Watch what pip does to opencv while installing it: mediapipe depends on
# opencv-contrib-python, and two opencv wheels in one environment is how
# `import cv2` starts failing in a way that looks like a code bug. If it
# does, reinstall the headless one on top:
#   pip install --force-reinstall opencv-python-headless

# The local voice, loaded once inside the robot (speech.tts.engine: piper_python).
# Pulls in onnxruntime, which the arcface embedder can use too.
piper-tts>=1.3
